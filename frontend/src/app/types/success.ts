export type UserSuccess = {
  success_id: string
  user_id: string
  success: {
    id_firebase: string
    name: string
    rarity: string
  }
}

export type Success = {
  id_firebase: string
  name: string
  rarity: string
}
